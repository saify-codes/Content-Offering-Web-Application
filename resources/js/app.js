import './bootstrap';
import 'flowbite'

import Alpine from 'alpinejs';

window.Alpine = Alpine;

Alpine.start();

// disable contextmenu
window.addEventListener('contextmenu', function (e) {
    e.preventDefault();
});


/* upload.blade.php */

/* upload.blade.php */
